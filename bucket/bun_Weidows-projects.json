{
    "version": "1.3.9",
    "description": "Incredibly fast JavaScript runtime, bundler, transpiler and package manager - all in one.",
    "homepage": "https://bun.sh/",
    "license": "MIT",
    "notes": [
        "主要解决:",
        "- main 里的 bun 并没有把 -g 的 bin 加入 PATH, 也没有 persist"
    ],
    "architecture": {
        "64bit": {
            "url": [
                "https://github.com/oven-sh/bun/releases/download/bun-v1.3.9/bun-windows-x64.zip",
                "https://github.com/oven-sh/bun/releases/download/bun-v1.3.9/bun-windows-x64-baseline.zip"
            ],
            "hash": [
                "f4c1cf3549f6af986dc6535c40b4785ff1a7e7805e59637ec450fc11adb0c874",
                "cc22962b3dcc51f279f13bf843675e052479e64a323f85eb29442066cf3942fe"
            ]
        }
    },
    "pre_install": [
        "$avx2 = Start-Job -ScriptBlock {",
        "    Add-Type -MemberDefinition '[DllImport(\"kernel32.dll\")] public static extern bool IsProcessorFeaturePresent(int ProcessorFeature);' -Name Kernel32 -Namespace Win32",
        "    return [Win32.Kernel32]::IsProcessorFeaturePresent(40)",
        "} | Receive-Job -Wait -AutoRemoveJob",
        "$target = 'bun-windows-x64'",
        "if (-not $avx2) {",
        "    $target += '-baseline'",
        "}",
        "Move-Item \"$dir\\$target\\bun.exe\" \"$dir\\bun.exe\"",
        "Remove-Item \"$dir\\bun-windows-*\" -Recurse",
        "",
        "mkdir ~/.bun -ErrorAction SilentlyContinue",
        "function Create-Junction { param ([string]$runtimeCache, [string]$runtimeCachePersist)",
        "  if (-not (Test-Path $runtimeCache)) { return }",
        "  if (Test-Path $runtimeCachePersist) {",
        "    Remove-Item $runtimeCache -Force -Recurse -ErrorAction SilentlyContinue",
        "    New-Item -Type Junction -Path $runtimeCache -Target $runtimeCachePersist | Out-Null",
        "  } else {",
        "    mkdir $runtimeCache -ErrorAction SilentlyContinue",
        "    Move-Item $runtimeCache $runtimeCachePersist -Force",
        "    New-Item -Type Junction -Path $runtimeCache -Target $runtimeCachePersist | Out-Null",
        "  }",
        "}",
        "foreach ($folder in @('.bun')) {",
        "  Create-Junction -runtimeCache \"$env:USERPROFILE\\$folder\" -runtimeCachePersist \"$persist_dir\\$folder\"",
        "}"
    ],
    "bin": [
        "bun.exe",
        [
            "bun.exe",
            "bunx",
            "x"
        ]
    ],
    "env_add_path": ".bun\\bin",
    "persist": ".bun",
    "checkver": {
        "github": "https://github.com/oven-sh/bun",
        "re": "bun-v([\\d.]+)"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": [
                    "https://github.com/oven-sh/bun/releases/download/bun-v$version/bun-windows-x64.zip",
                    "https://github.com/oven-sh/bun/releases/download/bun-v$version/bun-windows-x64-baseline.zip"
                ]
            }
        },
        "hash": {
            "url": "$baseurl/SHASUMS256.txt"
        }
    }
}
